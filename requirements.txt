opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
onnxruntime>=1.16.0
rembg[cpu]>=2.0.50
pytest>=7.0.0


# macOS on-device OCR (text_mode=ocr). Optional: the extractor falls back to
# text_mode=binarize when these are absent or the platform is not macOS.
pyobjc-framework-Vision>=10.0 ; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.0 ; sys_platform == "darwin"
